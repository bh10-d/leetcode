import (
    "slices"
)
func minimumSum(num int) int {
    nums := make([]int, 4)
    for i := 0; i < 4; i++ {
        nums[i] = num%10
        num = num/10
    }

    slices.Sort(nums)

    num1 := nums[0] * 10 + nums[2]
    num2 := nums[1] * 10 + nums[3]

    return num1+num2
}