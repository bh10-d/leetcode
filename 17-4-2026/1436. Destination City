Language: Golang

Phương pháp giải: Runtime: 0ms, Memory: 5.4MB


func destCity(paths [][]string) string {
    from := make(map[string]bool);

    for _, v := range paths {
        from[v[0]] = true
    }

    for _, v := range paths {
        if !from[v[1]] {
            return v[1];
        }
    }

    return "";
}