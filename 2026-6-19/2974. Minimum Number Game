func numberGame(nums []int) []int {
    arr := []int{}

    slices.Sort(nums)

    for i := 0; i < len(nums) - 1; i+=2 {
        alice := nums[i]
        bob := nums[i+1]
        arr = append(arr, bob)
        arr = append(arr, alice)
    }

    return arr
}