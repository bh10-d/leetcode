func findFinalValue(nums []int, original int) int {
    // sử dụng hashmap sau đó là loop vòng đầu tiên để đưa các giá trị vào hashmap
    // sau đó tại vòng lại thứ 2 bắt đầu duyệt

    m := make(map[int]bool)

    // for i := 0; i < len(nums); i++ {
    //     m[nums[i]] = true
    // }

    // total := original
    // for i:=0; i < len(nums);i++ {
    //     if !m[original] {
    //         return original
    //     }
    //     if _, ok := m[total]; ok {
    //         total = total * 2
    //     }
    // }
    // return total

    // vòng 1
    for _, v := range nums {
        m[v] = true
    }


    total := original

    // vòng 2 (total true thì nhân đôi nếu false thì return)
    for m[total] {
        total = total * 2
    }

    return total

}